# Local Multipass Ubuntu VM used to test the SSH deployment path for free,
# without a real cloud server. Not reachable from GitHub Actions runners
# (private network) - for local ansible-playbook testing only.
[ubuntu]
ansible-test-vm ansible_host=192.168.252.2 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/ansible_test_vm
